/**
 * Core domain types and interfaces.
 */

export interface CardData {
  id: string;
  title: string;
  meta: string;
  tag: string;
  price: string;
  imageUrl: string;
  imageAlt: string;
  rating: number;
}
